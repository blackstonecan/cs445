# BrainBlock Packing — Deep RL (CS 445/545)
# Direct dependencies only; pip resolves the rest (e.g. networkx, fsspec, sympy).
#
# torch is the CPU build. Install it from the PyTorch CPU index, e.g.:
#   pip install torch==2.12.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
torch==2.12.0
gymnasium==1.3.0
numpy==2.4.6
matplotlib==3.10.9
